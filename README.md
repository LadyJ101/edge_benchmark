# Edge Benchmark

Benchmarking lightweight vision models (MobileNetV3, MobileViT, EfficientFormer, Quantized CNN) for crop disease detection on low-cost ARM-based edge devices under Nigeria-inspired constraints.

## Project Structure
- models/ — model definitions
- datasets/ — dataset storage
- scripts/ — benchmarking scripts
- notebooks/ — experiments and testing
- results/ — logs and outputs
